# Cursor SDK (required for run_agent.py interactive/resume modes)
CURSOR_API_KEY=

# Local Docker MongoDB (default from docker-compose.yml)
MONGODB_URI=mongodb://localhost:27017

# Slack approval bot (Socket Mode — optional, for demo)
SLACK_BOT_TOKEN=
SLACK_APP_TOKEN=
SLACK_CHANNEL_ID=
